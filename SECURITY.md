# 安全与责任

## 这个工具做什么、不做什么

`relaycheck` 只用**你自己提供的** API Key，向**你自己正在使用**的中转站发只读的
`POST /v1/chat/completions` 请求。它不发消息、不改配置、不创建账号、不碰计费接口的写操作。
唯一的副作用是：**这些请求会真实计费到你自己的账户上**。

所以：

- 不要拿别人的 Key 跑它。
- `--probes context` 是唯一可能明显花钱的探针（每次请求动辄上万 token 输入），
  默认不跑，按需用。
- 先看 `--budget`（默认 240 秒墙钟）和 `--max-models`，再决定要不要上全量探针。

## 报告里有什么

报告（`report.md` / `report.json`）包含：

- 被测站的 URL、可用模型列表；
- 每条发现的证据：模型自报的名字、tokenizer 指纹、逐字比对样本、面板公开的上游成本等；
- 你**账户**的用量与成本数据（如果面板接口可达）。

报告里**没有**：

- 你的 API Key。`relaycheck/reporter.py` 全文不引用 `api_key`；报告只写目标 URL。
  这条可以在自己的仓库里直接验证：`grep -ri "api_key" relaycheck/reporter.py`。
- 你的 Authorization 请求头、cookie 或任何凭据。

**但账号数据仍然是敏感信息。** 「这个模型加价 41 倍」「余额 19.49 美元」是**你的**账单。
把一份完整报告贴到公开 issue 里，等于公开了你的消费记录和用量。分享前请先删掉
「面板」「计费」相关的段落。

## 关于结论本身

`relaycheck` 输出的是**证据**，不是判决。报告里每条发现都带 `severity`（有多严重）和
`confidence`（有多确定），两者刻意分开：

- `LOW` / `SUSPECTED` 的意思是「有个线索，但撑不起指控」；
- `HIGH` / `CONFIRMED` 的意思是「这个实验做成了，而且结果只能这么解释」。

用它去跟商家理论之前，请先读报告里的「诚实的局限」章节，以及每条发现的
「建议」——那里面写着要对方出示什么凭据。**我们只主张能证明的东西。**

## 报告本工具的漏洞

`relaycheck` 会对**不可信的第三方**返回的数据做解析：响应体、SSE 流、面板 JSON、
HTTP 头。凡是能被远端数据触发的本地问题都算安全问题，包括但不限于：

- 解析远端数据时崩溃、挂死、内存失控；
- 把远端可控内容写进报告并造成注入（Markdown / CSV / HTML）；
- 把 Key 泄漏进日志、报告、异常回溯或错误信息；
- 退出码混淆：让「没查成」看起来像「查过了」。

请在 issue 里说明版本（`relaycheck --version`）、操作系统、Python 版本，
以及一段能复现的最小输入。**不要附上真实 Key 或真实站的完整报告**——
用 `tests/mock_relay.py` 造一个最小复现即可（它就是为了这个存在的）：

```bash
python tests/mock_relay.py --scenario fraudulent --port 8200
```

## 支持范围

修复只进 `main`。0.1.x 是 alpha，接口（探针名、finding id、CLI 参数）仍可能变动；
finding id 一旦发布就尽量不重用，语义变更会写进 `CHANGELOG.md`。
